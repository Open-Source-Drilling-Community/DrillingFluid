apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingdrillingfluidwebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingdrillingfluidwebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingdrillingfluidwebappclient.selectorLabels" . | nindent 4 }}
